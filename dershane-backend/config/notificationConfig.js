module.exports = {
  attendance: "both",     // "email" | "push" | "both" | "none"
  examResult: "push",      // Öğrenciye deneme sonucu bildirimi
  schedule: "push",        // Ders programı / etkinlik bildirimi
  announcement: "push",    // Admin duyuruları
  homework: "push",        // İlerde eklerim belki
  general: "push"          // Genel sistem bildirimleri
};
